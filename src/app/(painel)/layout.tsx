/**
 * Layout isolado para o painel de chamadas da sala de espera.
 *
 * Não herda o DashboardLayout — sem Sidebar, sem Header, sem Toaster.
 * Ocupa o ecrã completo, destinado a ser projetado numa TV ou monitor.
 */
export default function PainelLayout({ children }: { children: React.ReactNode }) {
  return children;
}
